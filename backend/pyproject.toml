[project]
name = "allotment-svc"
version = "0"
requires-python = "==3.13.*"
dependencies = ["fastapi", "granian", "SQLAlchemy", "pydantic", "Authlib"]

[dependency-groups]
dev = ["fastapi[standard]", "pytest", "ruff", "mypy"]